# Content templates

`designs-template.csv` is a planning spreadsheet for organizing large image batches.

Separate multiple values with a vertical bar, for example:

`Summer|Spring`

The current website reads `assets/data.js`. The CSV is supplied to help prepare batches consistently before those rows are converted into design records.
